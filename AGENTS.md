# Agent 工程学习项目说明

本仓库是一套从零开始的 Agent 工程课程，不是一次性生成完整应用的项目。

## 每个新任务开始时必须执行

1. 阅读 `docs/learning/README.md`，确认课程版本和阶段映射。
2. 阅读 `docs/learning/PROGRESS.md`，确认当前阶段、已完成课程和下一课。
3. 如果用户指定“阶段 N”，只再阅读 `docs/learning/stages/0N_*.md` 对应阶段文件。
4. 本次聊天只推进一个阶段；不要同时实现其他阶段的课程内容。
5. 阶段内一次只教学一个 `.py` 文件和一个核心概念，完成验收后再进入下一课。

## 教学规则

- 使用中文教学，先解释心智模型，再展示最小代码。
- 每课遵循：复习 → 解释 → 最小实现 → 运行观察 → 用户练习 → 理解问答 → 更新进度。
- 不要预先生成整个阶段的代码文件；只创建当前课程需要的文件。
- 尽量让用户亲自解释和修改核心代码，不以大量代码代替教学。
- 用户未通过当前课验收时，不把课程或阶段标记为完成。
- 若用户要求复习，默认不修改进度，除非用户明确要求重新验收。

## 技术基线

- 只使用 `uv` 管理 Python、虚拟环境、依赖和运行命令。
- 使用 Python 3.12；运行课程文件使用 `uv run ...`。
- 不使用 Conda，不直接使用 `pip install`。
- OpenAI 主线使用 Responses API；兼容其他模型供应商时必须明确标出差异。
- API Key 只能来自环境变量或被忽略的 `.env` 文件，禁止写入代码、日志或聊天回复。
- 不读取或显示 `.env` 的真实内容；测试密钥逻辑时使用假值或只检查是否存在。

## 课程文件与进度

- 总路线：`docs/learning/README.md`
- 教学协议：`docs/learning/TEACHING_PROTOCOL.md`
- 唯一进度记录：`docs/learning/PROGRESS.md`
- 阶段定义：`docs/learning/stages/`
- 实验代码：`lessons/`

完成一课后更新 `PROGRESS.md`。变更课程编号、阶段范围或完成标准时，必须同步更新总路线、对应阶段文件和进度文件。

